#!/usr/bin/env python3

import sys
import math
import Board

class Path:
    boards = []

    def add(board):
        self.boards.append(board)
        return

    def clone(path):
        self.boards = path.boards
        return

    def last():
        return boards[len(boards)]

    def printPath():
        return
